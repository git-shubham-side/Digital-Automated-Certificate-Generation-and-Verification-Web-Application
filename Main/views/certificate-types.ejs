<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Select Certificate Template - TrustCert</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap"
      rel="stylesheet"
    />

    <style>
      * {
        font-family: "Inter", sans-serif;
      }

      body {
        background: #0a0a0f;
      }

      /* Gradient Background */
      .gradient-bg {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 0;
        opacity: 0.4;
      }

      .gradient-bg::before {
        content: "";
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background:
          radial-gradient(
            circle at 20% 30%,
            rgba(99, 102, 241, 0.15) 0%,
            transparent 50%
          ),
          radial-gradient(
            circle at 80% 70%,
            rgba(168, 85, 247, 0.15) 0%,
            transparent 50%
          );
        animation: gradientShift 20s ease infinite;
      }

      @keyframes gradientShift {
        0%,
        100% {
          transform: translate(0, 0) scale(1);
        }
        50% {
          transform: translate(-5%, -5%) scale(1.1);
        }
      }

      @keyframes fadeUp {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .fade-up {
        animation: fadeUp 0.6s ease forwards;
      }

      .glass {
        background: rgba(255, 255, 255, 0.03);
        backdrop-filter: blur(20px);
        border: 1px solid rgba(255, 255, 255, 0.1);
      }

      .template-card {
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
        overflow: hidden;
      }

      .template-card::before {
        content: "";
        position: absolute;
        inset: 0;
        border-radius: inherit;
        padding: 1px;
        background: linear-gradient(
          135deg,
          rgba(99, 102, 241, 0.3),
          rgba(168, 85, 247, 0.3)
        );
        -webkit-mask:
          linear-gradient(#fff 0 0) content-box,
          linear-gradient(#fff 0 0);
        -webkit-mask-composite: xor;
        mask-composite: exclude;
        opacity: 0;
        transition: opacity 0.4s ease;
      }

      .template-card:hover::before {
        opacity: 1;
      }

      .template-card:hover {
        transform: translateY(-8px);
        background: rgba(255, 255, 255, 0.05);
        box-shadow: 0 20px 60px rgba(99, 102, 241, 0.3);
      }

      .template-icon {
        width: 60px;
        height: 60px;
        border-radius: 16px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 28px;
        margin-bottom: 1rem;
        transition: all 0.3s ease;
      }

      .template-card:hover .template-icon {
        transform: scale(1.1) rotate(5deg);
      }

      .btn-primary {
        background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
      }

      .btn-primary::before {
        content: "";
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(
          90deg,
          transparent,
          rgba(255, 255, 255, 0.2),
          transparent
        );
        transition: left 0.5s ease;
      }

      .btn-primary:hover::before {
        left: 100%;
      }

      .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 30px rgba(99, 102, 241, 0.4);
      }

      .btn-secondary {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        transition: all 0.3s ease;
      }

      .btn-secondary:hover {
        background: rgba(255, 255, 255, 0.08);
        border-color: rgba(255, 255, 255, 0.2);
      }

      /* Custom Scrollbar */
      ::-webkit-scrollbar {
        width: 8px;
      }

      ::-webkit-scrollbar-track {
        background: #0a0a0f;
      }

      ::-webkit-scrollbar-thumb {
        background: linear-gradient(180deg, #6366f1, #8b5cf6);
        border-radius: 4px;
      }
    </style>
  </head>

  <body class="text-slate-200 min-h-screen">
    <!-- Animated Background -->
    <div class="gradient-bg"></div>

    <!-- HEADER -->
    <header
      class="relative z-10 border-b border-slate-800 glass px-6 md:px-8 py-5"
    >
      <div class="max-w-7xl mx-auto flex justify-between items-center">
        <div class="flex items-center gap-3">
          <div
            class="w-10 h-10 rounded-xl bg-gradient-to-br from-indigo-500 to-purple-600 flex items-center justify-center"
          >
            <svg
              class="w-5 h-5 text-white"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M4 5a1 1 0 011-1h14a1 1 0 011 1v2a1 1 0 01-1 1H5a1 1 0 01-1-1V5zM4 13a1 1 0 011-1h6a1 1 0 011 1v6a1 1 0 01-1 1H5a1 1 0 01-1-1v-6zM16 13a1 1 0 011-1h2a1 1 0 011 1v6a1 1 0 01-1 1h-2a1 1 0 01-1-1v-6z"
              />
            </svg>
          </div>
          <h1 class="text-xl md:text-2xl font-bold tracking-wide">
            Certificate Templates
          </h1>
        </div>

        <a
          href="index.html"
          class="btn-secondary px-5 py-2.5 rounded-lg text-sm font-medium flex items-center gap-2"
        >
          <svg
            class="w-4 h-4"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M10 19l-7-7m0 0l7-7m-7 7h18"
            />
          </svg>
          Back to Dashboard
        </a>
      </div>
    </header>

    <!-- MAIN -->
    <main class="relative z-10 p-6 md:p-10 max-w-7xl mx-auto">
      <!-- Page Description -->
      <div class="mb-10 text-center fade-up">
        <p class="text-slate-400 text-lg max-w-2xl mx-auto">
          Select a certificate template based on the type of event
        </p>
        <div
          class="mt-4 inline-flex items-center gap-2 px-4 py-2 bg-indigo-500/10 border border-indigo-500/30 rounded-full text-sm text-indigo-300"
        >
          <svg
            class="w-4 h-4"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
            />
          </svg>
          6 Professional Templates Available
        </div>
      </div>

      <!-- TEMPLATE GRID -->
      <div
        class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 md:gap-8"
      >
        <!-- SPORTS -->
        <div
          class="template-card glass rounded-2xl p-8 fade-up"
          style="animation-delay: 0.1s"
        >
          <div
            class="template-icon bg-gradient-to-br from-yellow-500 to-orange-600"
          >
            🏆
          </div>
          <h2 class="text-2xl font-bold mb-3">Sports Event</h2>
          <p class="text-slate-400 mb-6 text-sm leading-relaxed">
            Perfect for tournaments, competitions, winners & participants in
            athletic events.
          </p>

          <a
            href="sports-certificate-types.html"
            class="btn-primary inline-block w-full text-center py-3.5 rounded-xl font-semibold"
          >
            Use Template
          </a>
        </div>

        <!-- TECHNICAL -->
        <div
          class="template-card glass rounded-2xl p-8 fade-up"
          style="animation-delay: 0.2s"
        >
          <div
            class="template-icon bg-gradient-to-br from-blue-500 to-cyan-600"
          >
            💻
          </div>
          <h2 class="text-2xl font-bold mb-3">Technical Event</h2>
          <p class="text-slate-400 mb-6 text-sm leading-relaxed">
            Ideal for workshops, tech expos, hackathons, coding competitions &
            seminars.
          </p>

          <a
            href="certificate.html?type=technical"
            class="btn-primary inline-block w-full text-center py-3.5 rounded-xl font-semibold"
          >
            Use Template
          </a>
        </div>

        <!-- CULTURAL -->
        <div
          class="template-card glass rounded-2xl p-8 fade-up"
          style="animation-delay: 0.3s"
        >
          <div
            class="template-icon bg-gradient-to-br from-pink-500 to-rose-600"
          >
            🎭
          </div>
          <h2 class="text-2xl font-bold mb-3">Cultural / Gathering</h2>
          <p class="text-slate-400 mb-6 text-sm leading-relaxed">
            For annual day celebrations, cultural festivals, and college
            gatherings.
          </p>

          <a
            href="certificate.html?type=cultural"
            class="btn-primary inline-block w-full text-center py-3.5 rounded-xl font-semibold"
          >
            Use Template
          </a>
        </div>

        <!-- EXTRA ACTIVITY -->
        <div
          class="template-card glass rounded-2xl p-8 fade-up"
          style="animation-delay: 0.4s"
        >
          <div
            class="template-icon bg-gradient-to-br from-purple-500 to-indigo-600"
          >
            ⭐
          </div>
          <h2 class="text-2xl font-bold mb-3">Extra Activity</h2>
          <p class="text-slate-400 mb-6 text-sm leading-relaxed">
            Designed for NSS, volunteering programs, social work & club
            activities.
          </p>

          <a
            href="certificate.html?type=extra"
            class="btn-primary inline-block w-full text-center py-3.5 rounded-xl font-semibold"
          >
            Use Template
          </a>
        </div>

        <!-- PARTICIPATION -->
        <div
          class="template-card glass rounded-2xl p-8 fade-up"
          style="animation-delay: 0.5s"
        >
          <div
            class="template-icon bg-gradient-to-br from-green-500 to-emerald-600"
          >
            📜
          </div>
          <h2 class="text-2xl font-bold mb-3">Participation</h2>
          <p class="text-slate-400 mb-6 text-sm leading-relaxed">
            General participation certificates for all types of events and
            activities.
          </p>

          <a
            href="certificate.html?type=participation"
            class="btn-primary inline-block w-full text-center py-3.5 rounded-xl font-semibold"
          >
            Use Template
          </a>
        </div>

        <!-- APPRECIATION -->
        <div
          class="template-card glass rounded-2xl p-8 fade-up"
          style="animation-delay: 0.6s"
        >
          <div class="template-icon bg-gradient-to-br from-red-500 to-pink-600">
            🎖
          </div>
          <h2 class="text-2xl font-bold mb-3">Appreciation</h2>
          <p class="text-slate-400 mb-6 text-sm leading-relaxed">
            Recognition and appreciation certificates for outstanding
            contributions.
          </p>

          <a
            href="certificate.html?type=appreciation"
            class="btn-primary inline-block w-full text-center py-3.5 rounded-xl font-semibold"
          >
            Use Template
          </a>
        </div>
      </div>

      <!-- Help Section -->
      <div
        class="mt-12 glass rounded-2xl p-8 max-w-3xl mx-auto text-center fade-up"
        style="animation-delay: 0.7s"
      >
        <div
          class="w-14 h-14 rounded-xl bg-gradient-to-br from-indigo-500 to-purple-600 flex items-center justify-center mx-auto mb-4"
        >
          <svg
            class="w-7 h-7 text-white"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
            />
          </svg>
        </div>
        <h3 class="text-xl font-bold mb-3">Need Help Choosing?</h3>
        <p class="text-slate-400 mb-6 text-sm">
          Each template is professionally designed and can be customized with
          your event details. All certificates include QR codes for instant
          verification.
        </p>
        <button
          class="btn-secondary px-8 py-3 rounded-lg font-semibold text-sm"
        >
          Contact Support
        </button>
      </div>
    </main>

    <!-- FOOTER -->
    <footer
      class="relative z-10 text-center text-xs text-slate-500 py-8 border-t border-slate-800"
    >
      <p class="mb-2">MGM College of CS & IT, Nanded</p>
      <p class="text-slate-600">Certificate Management & Verification System</p>
      <div class="mt-4 flex justify-center gap-4">
        <a href="#" class="text-slate-500 hover:text-slate-300 transition"
          >Help</a
        >
        <span class="text-slate-700">•</span>
        <a href="#" class="text-slate-500 hover:text-slate-300 transition"
          >Documentation</a
        >
        <span class="text-slate-700">•</span>
        <a href="#" class="text-slate-500 hover:text-slate-300 transition"
          >Contact</a
        >
      </div>
    </footer>
  </body>
</html>
